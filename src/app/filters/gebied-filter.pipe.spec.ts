import { GebiedFilterPipe } from './gebied-filter.pipe';

describe('GebiedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GebiedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
