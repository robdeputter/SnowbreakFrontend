import { RankingFilterPipe } from './ranking-filter.pipe';

describe('RankingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RankingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
